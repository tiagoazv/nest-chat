{
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.0.1"
  },
  "scripts": {
    "start": "npm run build --workspace frontend && npm run build --workspace backend && concurrently \"npm run start --workspace backend\" \"npm run start --workspace frontend\"",
    "dev": "concurrently \"npm run start --workspace backend\" \"npm run dev --workspace frontend\""
  },
  "dependencies": {
    "@nestjs/class-validator": "^0.13.4",
    "@nestjs/mongoose": "^11.0.3",
    "autoprefixer": "^10.4.21",
    "class-validator": "^0.14.2",
    "mongoose": "^8.18.0",
    "moongoose": "^0.0.1",
    "postcss": "^8.5.6",
    "reflect-metadata": "^0.2.2"
  }
}
